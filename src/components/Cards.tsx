import type { Profile } from '../types/sharedTypes'

interface Props {
    profile: Profile
}

export default function Cards (props: Props) {
    return (
        <>
            <p> Cards goes here </p>
        </>
    )
}